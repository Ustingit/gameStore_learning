@using GameStore.HtmlHelpers
@using GameStore.StoreDomain.Entities

@model GameStore.Models.GamesListViewModel

@{
    ViewBag.Title = "Товары";
}

@foreach (var game in Model.Games)
{
    <div>
        <h3>@game.Name</h3>
        <p>@game.Description</p>
        <h4>@game.Price.ToString("# руб")</h4>
    </div>
}

<div>@Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x }))</div>